import { BriefingForm } from "./components/BriefingForm";

function App() {
  return (
    <div className="min-h-screen bg-brand-dark bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-brand-dark via-brand-dark to-black text-white selection:bg-brand-accent selection:text-brand-dark">
      <BriefingForm />
    </div>
  );
}

export default App;
